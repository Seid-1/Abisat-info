<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title1}}</title>

  <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/lux/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <link rel="stylesheet" href="/css/style.css" />
  <link rel="shortcut icon" href="/images/contact us.jpg" type="image/x-icon" />

</head>

<body class="d-flex flex-column min-vh-100">
  {{> navbar}}

  <section class="contact-section">
    <div class="container py-5">
      <div class="text-center mb-4 text-white">
        <h2 class="fw-bold text-white">Contact <span class="text-white">Us</span> 📩</h2>
        <p class="lead">We'd love to hear from you!</p>
      </div>

      {{#if successMessage}}
        <div class="alert alert-success mt-3 text-center">{{successMessage}}</div>
      {{/if}}

      {{#if errorMessage}}
        <div class="alert alert-danger mt-3 text-center">{{errorMessage}}</div>
      {{/if}}

      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
          <form class="p-4 shadow-lg bg_color text-white rounded-4" action="/send-message" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Your Name</label>
              <input
                type="text"
                class="form-control dark-input"
                id="name"
                name="name"
                placeholder="Enter your name"
                required
              />
            </div>
            <div class="mb-3">
              <label for="email" class="form-label">Email address</label>
              <input
                type="email"
                class="form-control dark-input"
                id="email"
                name="email"
                placeholder="name@gmail.com"
                required
              />
            </div>
            <div class="mb-3">
              <label for="message" class="form-label">Message</label>
              <textarea
                class="form-control dark-input"
                id="message"
                name="message"
                rows="4"
                placeholder="Your message here..."
                required
              ></textarea>
            </div>
            <button type="submit" class="btn btn-primary w-100">Send Message</button>
          </form>
        </div>
      </div>

      <div class="text-center mt-4 text-white">
        <h5 class="text-white">Reach us on</h5>
        <a href="https://t.me/AbisatEthiopia" target="_blank" class="text-decoration-none mx-2 fs-3 text-info">
          <i class="fa-brands fa-telegram fa-bounce"></i>
        </a>

        <a href="https://youtube.com/@AbisatEthiopia" class="text-decoration-none mx-2 fs-3" style="color: #FF0000;">
          <i class="fa-brands fa-youtube fa-beat"></i>
        </a>

        <a href="https://facebook.com/Abisatinfo1" class="text-decoration-none text-white mx-2 fs-3">
          <i class="fa-brands fa-facebook fa-beat-fade"></i>
        </a>    
    
        <a href="mailto:abisatinfo@gmail.com" class="text-decoration-none mx-2 fs-3 text-danger">
          <i class="fa-solid fa-envelope fa-flip"></i>
        </a>

        <a href="tel:+251911837202" class="text-white fs-4" aria-label="Phone">
        <i class="fas fa-phone fa-shake" style="color: #4CAF50;"></i>
      </a>

      </div>
    </div>
  </section>

  {{> footer}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
