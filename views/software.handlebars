<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title3}}</title>

  <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/lux/bootstrap.min.css" rel="stylesheet" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <link rel="stylesheet" href="/css/style.css" />
  <link rel="shortcut icon" href="/images/swbox.png" type="image/x-icon" />

</head>
<body class="d-flex flex-column min-vh-100">

  {{> navbar}}
<div class="container py-5 mt-navbar-offset">
  <h3 class="mb-4 fw-bold text-center text-white">📦 Software Downloads</h3>
  <div class="table-responsive">
    <table
      class="table text-white align-middle shadow bg_color rounded-4"
      style="border-collapse: separate; border-spacing: 0 0.75rem;"
    >
      <thead>
        <tr>
          <th class="text-info margin_custom">Brand</th>
          <th class="text-info ps-4">Model</th>
          <th class="text-info ps-4">File</th>
          <th class="text-info ps-4">Description</th>
          <th class="text-info ps-4">Action</th>
        </tr>
      </thead>
      <tbody>
        {{#each files}}
          <tr>
            <td>{{this.brand}}</td>
            <td><b>{{this.model}}</b></td>
            <td>{{this.filename}}</td>
            <td>{{this.description}}</td>
            <td>
              <a href="/download/software?download_url={{this.download_url}}" class="btn btn-sm btn-outline-info rounded-pill">
                📥 Download
              </a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div><br>

  <hr class="my-4" style="border-top: 1px solid #aaa;" />
    <div class="text-center">
      <a href="https://t.me/Abisatinfo1" class="btn btn-outline-light btn-sm px-4">For More</a>
    </div>
</div>

</body>
{{> footer}}
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
