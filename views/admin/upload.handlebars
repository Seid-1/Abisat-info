<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Uploading Files</title>

  <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/lux/bootstrap.min.css" rel="stylesheet" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <link rel="stylesheet" href="/css/style.css" />
 <link rel="shortcut icon" href="/images/upload.jpg" type="image/x-icon" />
  
</head>
<body class="d-flex flex-column min-vh-100">

  {{> navbar}}

  <section class="contact-section">
  <div class="container py-5 mt-5">
    <h3 class="text-center fw-bold mb-4 text-white animate__animated animate__fadeInDown">
      <i class="bi bi-upload"></i> Upload New File
    </h3>

    {{#if message}}
      <div class="alert alert-info text-center fw-semibold">
        {{message}}
      </div>
    {{/if}}

    <form
      action="/admin/upload"
      method="POST"
      enctype="multipart/form-data"
      class="p-4 bg_color text-white border-0 rounded-4 shadow-lg animate__animated animate__fadeInUp"
    >
      <div class="row g-4">
        <div class="col-md-6">
          <label for="brand" class="form-label text-white fw-semibold">Brand</label>
          <input
            type="text"
            name="brand"
            id="brand"
            class="form-control"
            placeholder="e.g. LifeStar"
            required
          />
        </div>

        <div class="col-md-6">
          <label for="model" class="form-label text-white fw-semibold">Model</label>
          <input
            type="text"
            name="model"
            id="model"
            class="form-control"
            placeholder="e.g. LS-1000"
            required
          />
        </div>

        <div class="col-md-6">
          <label for="file_type" class="form-label text-white fw-semibold">File Type</label>
          <select
            name="file_type"
            id="file_type"
            class="form-select"
            required
          >
            <option disabled selected>Choose type...</option>
            <option value="SW">Software</option>
            <option value="Loader">Loader</option>
          </select>
        </div>

        <div class="col-md-6">
          <label for="file" class="form-label text-white fw-semibold">Choose File</label>
          <input
            type="file"
            name="file"
            id="file"
            class="form-control"
            required
          />
        </div>

        <div class="col-md-6">
          <label for="description" class="form-label text-white fw-semibold">Description</label>
          <input
            type="text"
            name="description"
            id="description"
            class="form-control"
            placeholder="e.g. This Software is for ..."
            required
          />
        </div>
      </div>

      <div class="text-center mt-4">
        <button type="submit" class="btn btn-outline-light px-4 fw-semibold rounded-pill shadow-sm">
          📁 Upload File
        </button>
      </div>
    </form>
  </div>
</section>

  {{> footer}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
